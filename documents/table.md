CREATE TABLE `jobs` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `queue` varchar(255) NOT NULL,
  `payload` longtext NOT NULL,
  `attempts` tinyint(3) unsigned NOT NULL,
  `reserved` tinyint(3) unsigned DEFAULT NULL,
  `reserved_at` int(10) unsigned DEFAULT NULL,
  `available_at` int(10) unsigned NOT NULL,
  `created_at` int(10) unsigned NOT NULL,
  PRIMARY KEY (`id`),
  KEY `queue` (`queue`)
) ENGINE=InnoDB AUTO_INCREMENT=65 DEFAULT CHARSET=utf8mb3 COLLATE=utf8mb3_unicode_ci;


CREATE TABLE `jobs_apply` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `job_id` int(10) unsigned NOT NULL,
  `owner` smallint(5) unsigned DEFAULT 1,
  `mid` int(10) unsigned NOT NULL,
  `comp_id` int(10) unsigned NOT NULL,
  `resume_id` int(10) unsigned NOT NULL DEFAULT 0,
  `created` int(11) DEFAULT NULL,
  `source_id` int(10) unsigned DEFAULT 0,
  `rating` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `views` smallint(3) unsigned DEFAULT 0 COMMENT 'Số lượt view',
  `resume_title` varchar(250) DEFAULT NULL,
  `attach_file` tinytext NOT NULL,
  `letter_title` tinytext DEFAULT NULL,
  `letter_content` text DEFAULT NULL,
  `log_data` tinytext DEFAULT NULL COMMENT 'Log IP, city, country, ... when apply',
  `wfid` int(10) unsigned NOT NULL DEFAULT 0,
  `hired` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `note` text DEFAULT NULL,
  `status` tinyint(4) NOT NULL,
  `created_by` int(11) DEFAULT NULL,
  `updated_by` int(11) DEFAULT NULL,
  `created_at` int(11) DEFAULT NULL,
  `updated_at` int(11) DEFAULT NULL,
  `reason` tinyint(4) NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY `job_id` (`job_id`,`mid`),
  KEY `owner_id` (`owner`),
  KEY `resume_id` (`resume_id`),
  KEY `wfid` (`wfid`),
  KEY `comp_id` (`comp_id`)
) ENGINE=InnoDB AUTO_INCREMENT=34976 DEFAULT CHARSET=utf8mb3 COLLATE=utf8mb3_general_ci;


CREATE TABLE `job_members` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `owner` int(10) unsigned NOT NULL,
  `created` int(10) unsigned DEFAULT NULL,
  `modified` int(10) unsigned DEFAULT NULL,
  `email` varchar(50) NOT NULL,
  `phone` varchar(50) DEFAULT NULL,
  `mobile` varchar(20) DEFAULT NULL,
  `firstname` varchar(40) NOT NULL,
  `lastname` varchar(30) NOT NULL,
  `fullname` varchar(100) DEFAULT NULL,
  `password` varchar(100) DEFAULT NULL,
  `status` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `auth_key` varchar(50) DEFAULT NULL,
  `last_login` int(255) DEFAULT NULL,
  `source_type` tinyint(3) unsigned NOT NULL,
  `address` tinytext NOT NULL,
  `birthday` date DEFAULT NULL,
  `avatar` tinytext NOT NULL,
  `followed` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `gender` tinyint(3) unsigned NOT NULL DEFAULT 0 COMMENT '1:man; 2:woman',
  `marital` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `ctid` int(10) unsigned DEFAULT NULL,
  `nationality` varchar(2) NOT NULL DEFAULT '0',
  `source` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `ratings` int(10) unsigned NOT NULL DEFAULT 0,
  `avg_rating` float NOT NULL DEFAULT 0,
  `jobs` int(11) NOT NULL DEFAULT 0,
  `have_resume` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `latitude` decimal(12,6) DEFAULT NULL,
  `longitude` decimal(12,6) DEFAULT NULL,
  `images` tinytext DEFAULT NULL,
  `identity` varchar(30) DEFAULT NULL,
  `industries` varchar(50) DEFAULT NULL,
  `locations` varchar(50) DEFAULT NULL,
  `refer_code` varchar(50) DEFAULT NULL,
  `cmnd` varchar(45) DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `id_user_email` (`email`),
  KEY `owner` (`owner`)
) ENGINE=MyISAM AUTO_INCREMENT=33549 DEFAULT CHARSET=utf8mb3 COLLATE=utf8mb3_general_ci;

CREATE TABLE `jobs_apply` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `job_id` int(10) unsigned NOT NULL,
  `owner` smallint(5) unsigned DEFAULT 1,
  `mid` int(10) unsigned NOT NULL,
  `comp_id` int(10) unsigned NOT NULL,
  `resume_id` int(10) unsigned NOT NULL DEFAULT 0,
  `created` int(11) DEFAULT NULL,
  `source_id` int(10) unsigned DEFAULT 0,
  `rating` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `views` smallint(3) unsigned DEFAULT 0 COMMENT 'Số lượt view',
  `resume_title` varchar(250) DEFAULT NULL,
  `attach_file` tinytext NOT NULL,
  `letter_title` tinytext DEFAULT NULL,
  `letter_content` text DEFAULT NULL,
  `log_data` tinytext DEFAULT NULL COMMENT 'Log IP, city, country, ... when apply',
  `wfid` int(10) unsigned NOT NULL DEFAULT 0,
  `hired` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `note` text DEFAULT NULL,
  `status` tinyint(4) NOT NULL,
  `created_by` int(11) DEFAULT NULL,
  `updated_by` int(11) DEFAULT NULL,
  `created_at` int(11) DEFAULT NULL,
  `updated_at` int(11) DEFAULT NULL,
  `reason` tinyint(4) NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY `job_id` (`job_id`,`mid`),
  KEY `owner_id` (`owner`),
  KEY `resume_id` (`resume_id`),
  KEY `wfid` (`wfid`),
  KEY `comp_id` (`comp_id`)
) ENGINE=InnoDB AUTO_INCREMENT=34976 DEFAULT CHARSET=utf8mb3 COLLATE=utf8mb3_general_ci;


CREATE TABLE `news` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `owner` int(10) unsigned NOT NULL,
  `lang` varchar(3) DEFAULT NULL,
  `catid` varchar(100) DEFAULT NULL,
  `created` int(10) unsigned NOT NULL,
  `modified` int(10) unsigned NOT NULL,
  `status` tinyint(4) NOT NULL,
  `type` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `is_top` tinyint(3) unsigned NOT NULL DEFAULT 0,
  `hits` int(10) unsigned NOT NULL DEFAULT 0,
  `title` varchar(250) NOT NULL,
  `image` tinytext NOT NULL,
  `intro` text NOT NULL,
  `source` varchar(100) DEFAULT NULL,
  `content` text NOT NULL,
  `seo_title` tinytext DEFAULT NULL,
  `seo_description` tinytext DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `owner` (`owner`),
  KEY `catid` (`catid`),
  KEY `lang` (`lang`),
  KEY `status` (`status`,`type`)
) ENGINE=MyISAM AUTO_INCREMENT=14 DEFAULT CHARSET=utf8mb3 COLLATE=utf8mb3_general_ci;